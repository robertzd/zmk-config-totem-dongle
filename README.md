# TOTEM Dongle ZMK Config

Personal ZMK config for the [TOTEM](https://github.com/GEIGEIGEIST/TOTEM) keyboard with a third dongle setup built around [Prospector](https://github.com/carrefinho/prospector). This repo contains the firmware configuration, local shield files, and personal keymap customizations for my setup, including Norwegian key definitions, dual-OS layers, host switching, and a few dongle-specific tweaks.

Detailed hardware information belongs in the upstream TOTEM and Prospector repos.

## Based On / Related Projects

- [TOTEM](https://github.com/GEIGEIGEIST/TOTEM) - base keyboard hardware/project.
- [ZMK](https://github.com/zmkfirmware/zmk) - firmware and build system.
- [Prospector](https://github.com/carrefinho/prospector) - dongle/display hardware platform.
- [prospector-zmk-module](https://github.com/carrefinho/prospector-zmk-module) - ZMK module used for Prospector integration.
- [manna-harbour/miryoku](https://github.com/manna-harbour/miryoku) - inspiration for the keymap layout.

## What This Repo Builds

From [`build.yaml`](build.yaml):

- `totem_left`
- `totem_right`
- `totem_dongle prospector_adapter`
- `settings_reset`

These are the firmware images for the left half, right half, dongle, and settings reset target.

Firmware artifacts are generated by the GitHub Actions workflow in [`.github/workflows/build.yml`](.github/workflows/build.yml), which runs the upstream ZMK user-config build for the targets listed in [`build.yaml`](build.yaml).

## What Is Customized

- Local TOTEM shield files and the dongle target in [`config/boards/shields/totem`](config/boards/shields/totem).
- Dongle-specific Prospector and display settings such as fixed brightness, display rotation, and split-central battery proxying.
- Norwegian key definitions in [`config/keys_nb.h`](config/keys_nb.h).
- Personal keymap customizations and behaviors in [`config/totem.keymap`](config/totem.keymap).
- Dual-OS Mac layers, host switching, ZMK Studio unlock, and gaming layers.
- Minor BLE and radio tuning in [`config/totem.conf`](config/totem.conf).

## Layout Reference

Generated layout references live in [`docs/layouts`](docs/layouts):

- Main layers PNG: [`docs/layouts/totem-layouts-main.png`](docs/layouts/totem-layouts-main.png)
- Main layers SVG: [`docs/layouts/totem-layouts-main.svg`](docs/layouts/totem-layouts-main.svg)
- Full layout PNG: [`docs/layouts/totem-layouts.png`](docs/layouts/totem-layouts.png)
- Full layout SVG: [`docs/layouts/totem-layouts.svg`](docs/layouts/totem-layouts.svg)
- Parsed layout YAML: [`docs/layouts/totem-layouts.yaml`](docs/layouts/totem-layouts.yaml)

The main reference focuses on the layers used most often day to day: `Base`, `Nav`, `Num`, `Sym`, `Meta`, and `Media`. The full reference also includes the gaming and Mac-specific layers.

![TOTEM main layout reference](docs/layouts/totem-layouts-main.png)

These references are generated with [keymap-drawer](https://github.com/caksoylar/keymap-drawer) through [`scripts/render_layouts.py`](scripts/render_layouts.py). The script adds the repo-specific processing that keymap-drawer alone does not provide: it parses [`config/totem.keymap`](config/totem.keymap), normalizes legends for the Norwegian and custom bindings used in this repo, and generates both the full layout renders and the reduced main-layer renders. The helper script was created with Codex/ChatGPT assistance.

To regenerate the images after editing the keymap:

```sh
python3 -m pip install --target .tools keymap-drawer
python3 scripts/render_layouts.py
```

## Upstream Refs

- [`zmkfirmware/zmk`](https://github.com/zmkfirmware/zmk): `main`
- [`carrefinho/prospector-zmk-module`](https://github.com/carrefinho/prospector-zmk-module): `feat/new-status-screens`
- Build workflow: `zmkfirmware/zmk/.github/workflows/build-user-config.yml@main`

These are the tracked refs and branches from [`config/west.yml`](config/west.yml) and [`.github/workflows/build.yml`](.github/workflows/build.yml), not tagged releases.

## Where To Edit

- [`config/totem.keymap`](config/totem.keymap)
- [`config/totem.conf`](config/totem.conf)
- [`config/west.yml`](config/west.yml)
- [`config/boards/shields/totem`](config/boards/shields/totem)
